#pragma once

#include <string>

typedef enum RequestTypes {
    REQUEST_AUDIO = 0,
    REQUEST_GRAPHICS = 1,
    REQUEST_INPUT = 2,
    REQUEST_PHYSICS = 3,
    REQUEST_NETWORK = 4,
    REQUEST_ACTIONS = 5
};