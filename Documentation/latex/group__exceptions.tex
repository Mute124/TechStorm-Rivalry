\doxysection{Exceptions}
\hypertarget{group__exceptions}{}\label{group__exceptions}\index{Exceptions@{Exceptions}}
\doxysubsubsection*{Namespaces}
\begin{DoxyCompactItemize}
\item 
namespace \mbox{\hyperlink{namespacestd_1_1____exception__ptr}{std\+::\+\_\+\+\_\+exception\+\_\+ptr}}
\end{DoxyCompactItemize}
\doxysubsubsection*{Classes}
\begin{DoxyCompactItemize}
\item 
class \mbox{\hyperlink{classstd_1_1exception}{std\+::exception}}
\begin{DoxyCompactList}\small\item\em Base class for all library exceptions. \end{DoxyCompactList}\item 
class \mbox{\hyperlink{classstd_1_1____exception__ptr_1_1exception__ptr}{std\+::\+\_\+\+\_\+exception\+\_\+ptr\+::exception\+\_\+ptr}}
\begin{DoxyCompactList}\small\item\em An opaque pointer to an arbitrary exception. \end{DoxyCompactList}\end{DoxyCompactItemize}
\doxysubsubsection*{Functions}
\begin{DoxyCompactItemize}
\item 
\mbox{\hyperlink{classstd_1_1____exception__ptr_1_1exception__ptr}{exception\+\_\+ptr}} \mbox{\hyperlink{group__exceptions_gafcca609a0e064086e3d37ff25c85ab69}{std\+::current\+\_\+exception}} () \+\_\+\+GLIBCXX\+\_\+\+USE\+\_\+\+NOEXCEPT
\item 
{\footnotesize template$<$typename \+\_\+\+Ex $>$ }\\\mbox{\hyperlink{classstd_1_1____exception__ptr_1_1exception__ptr}{exception\+\_\+ptr}} \mbox{\hyperlink{group__exceptions_ga90822fbbd4a65b89480fb64d67fc69b6}{std\+::make\+\_\+exception\+\_\+ptr}} (\+\_\+\+Ex) \+\_\+\+GLIBCXX\+\_\+\+USE\+\_\+\+NOEXCEPT
\item 
\mbox{\hyperlink{_classes_2lib_2raylib_2src_2external_2_a_n_g_l_e_2_e_g_l_2eglext_8h_a0f7193911e632cd84da4082d5c628fe3}{void}} \mbox{\hyperlink{group__exceptions_ga64d0b68338d7edbfd7d95f4177dbc442}{std\+::rethrow\+\_\+exception}} (\mbox{\hyperlink{classstd_1_1____exception__ptr_1_1exception__ptr}{exception\+\_\+ptr}}) \mbox{\hyperlink{group__exceptions_gab4148f172d910c82bf981da4f900c4a4}{\+\_\+\+\_\+attribute\+\_\+\+\_\+}}((\+\_\+\+\_\+noreturn\+\_\+\+\_\+))
\begin{DoxyCompactList}\small\item\em Throw the object pointed to by the exception\+\_\+ptr. \end{DoxyCompactList}\item 
{\footnotesize template$<$typename \+\_\+\+Ex $>$ }\\\mbox{\hyperlink{group__exceptions_gab4148f172d910c82bf981da4f900c4a4}{std\+::\+\_\+\+\_\+attribute\+\_\+\+\_\+}} ((\+\_\+\+\_\+always\+\_\+inline\+\_\+\+\_\+)) \mbox{\hyperlink{classstd_1_1____exception__ptr_1_1exception__ptr}{exception\+\_\+ptr}} \mbox{\hyperlink{group__exceptions_ga90822fbbd4a65b89480fb64d67fc69b6}{make\+\_\+exception\+\_\+ptr}}(\+\_\+\+Ex) \+\_\+\+GLIBCXX\+\_\+\+USE\+\_\+\+NOEXCEPT
\begin{DoxyCompactList}\small\item\em Obtain an exception\+\_\+ptr pointing to a copy of the supplied object. \end{DoxyCompactList}\end{DoxyCompactItemize}
\begin{DoxyCompactItemize}
\item 
namespace std \mbox{\hyperlink{group__exceptions_ga544d74b2d5d0dd2197f5fd6f02691de0}{\+\_\+\+GLIBCXX\+\_\+\+VISIBILITY}} (default)
\end{DoxyCompactItemize}


\doxysubsection{Detailed Description}
\begin{DoxySince}{Since}
C++98
\end{DoxySince}
Classes and functions for reporting errors via exceptions. 

\doxysubsection{Function Documentation}
\Hypertarget{group__exceptions_gab4148f172d910c82bf981da4f900c4a4}\label{group__exceptions_gab4148f172d910c82bf981da4f900c4a4} 
\index{Exceptions@{Exceptions}!\_\_attribute\_\_@{\_\_attribute\_\_}}
\index{\_\_attribute\_\_@{\_\_attribute\_\_}!Exceptions@{Exceptions}}
\doxysubsubsection{\texorpdfstring{\_\_attribute\_\_()}{\_\_attribute\_\_()}}
{\footnotesize\ttfamily template$<$typename \+\_\+\+Ex $>$ \\
std\+::\+\_\+\+\_\+attribute\+\_\+\+\_\+ (\begin{DoxyParamCaption}\item[{(\+\_\+\+\_\+always\+\_\+inline\+\_\+\+\_\+)}]{ }\end{DoxyParamCaption})}



Obtain an exception\+\_\+ptr pointing to a copy of the supplied object. 

\Hypertarget{group__exceptions_ga544d74b2d5d0dd2197f5fd6f02691de0}\label{group__exceptions_ga544d74b2d5d0dd2197f5fd6f02691de0} 
\index{Exceptions@{Exceptions}!\_GLIBCXX\_VISIBILITY@{\_GLIBCXX\_VISIBILITY}}
\index{\_GLIBCXX\_VISIBILITY@{\_GLIBCXX\_VISIBILITY}!Exceptions@{Exceptions}}
\doxysubsubsection{\texorpdfstring{\_GLIBCXX\_VISIBILITY()}{\_GLIBCXX\_VISIBILITY()}}
{\footnotesize\ttfamily namespace std \+\_\+\+GLIBCXX\+\_\+\+VISIBILITY (\begin{DoxyParamCaption}\item[{default}]{ }\end{DoxyParamCaption})}

The base of the I/O class hierarchy.

This class defines everything that can be defined about I/O that does not depend on the type of characters being input or output. Most people will only see {\ttfamily ios\+\_\+base} when they need to specify the full name of the various I/O flags (e.\+g., the openmodes).

These are thrown to indicate problems with io.

27.\+4.\+2.\+1.\+1 Class ios\+\_\+base\+::failure

This is a bitmask type.

{\ttfamily {\itshape \+\_\+\+Ios\+\_\+\+Fmtflags} is} implementation-\/defined, but it is valid to perform bitwise operations on these values and expect the Right Thing to happen. Defined objects of type fmtflags are\+:
\begin{DoxyItemize}
\item boolalpha
\item dec
\item fixed
\item hex
\item internal
\item left
\item oct
\item right
\item scientific
\item showbase
\item showpoint
\item showpos
\item skipws
\item unitbuf
\item uppercase
\item adjustfield
\item basefield
\item floatfield
\end{DoxyItemize}

Insert/extract {\ttfamily bool} in alphabetic rather than numeric format.

Converts integer input or generates integer output in decimal base.

Generate floating-\/point output in fixed-\/point notation.

Converts integer input or generates integer output in hexadecimal base.

Adds fill characters at a designated internal point in certain generated output, or identical to {\ttfamily right} if no such point is designated.

Adds fill characters on the right (final positions) of certain generated output. (I.\+e., the thing you print is flush left.)

Converts integer input or generates integer output in octal base.

Adds fill characters on the left (initial positions) of certain generated output. (I.\+e., the thing you print is flush right.)

Generates floating-\/point output in scientific notation.

Generates a prefix indicating the numeric base of generated integer output.

Generates a decimal-\/point character unconditionally in generated floating-\/point output.

Generates a + sign in non-\/negative generated numeric output.

Skips leading white space before certain input operations.

Flushes output after each output operation.

Replaces certain lowercase letters with their uppercase equivalents in generated output.

A mask of left\texorpdfstring{$\vert$}{|}right\texorpdfstring{$\vert$}{|}internal. Useful for the 2-\/arg form of {\ttfamily setf}.

A mask of dec\texorpdfstring{$\vert$}{|}oct\texorpdfstring{$\vert$}{|}hex. Useful for the 2-\/arg form of {\ttfamily setf}.

A mask of scientific\texorpdfstring{$\vert$}{|}fixed. Useful for the 2-\/arg form of {\ttfamily setf}.

This is a bitmask type.

{\ttfamily {\itshape \+\_\+\+Ios\+\_\+\+Iostate} is} implementation-\/defined, but it is valid to perform bitwise operations on these values and expect the Right Thing to happen. Defined objects of type iostate are\+:
\begin{DoxyItemize}
\item badbit
\item eofbit
\item failbit
\item goodbit
\end{DoxyItemize}

Indicates a loss of integrity in an input or output sequence (such as an irrecoverable read error from a file).

Indicates that an input operation reached the end of an input sequence.

Indicates that an input operation failed to read the expected characters, or that an output operation failed to generate the desired characters.

Indicates all is well.

This is a bitmask type.

{\ttfamily {\itshape \+\_\+\+Ios\+\_\+\+Openmode} is} implementation-\/defined, but it is valid to perform bitwise operations on these values and expect the Right Thing to happen. Defined objects of type openmode are\+:
\begin{DoxyItemize}
\item app
\item ate
\item binary
\item in
\item out
\item trunc
\end{DoxyItemize}

Seek to end before each write.

Open and seek to end immediately after opening.

Perform input and output in binary mode (as opposed to text mode). This is probably not what you think it is; see \href{https://gcc.gnu.org/onlinedocs/libstdc++/manual/fstreams.html\#std.io.filestreams.binary}{\texttt{ https\+://gcc.\+gnu.\+org/onlinedocs/libstdc++/manual/fstreams.\+html\#std.\+io.\+filestreams.\+binary}}

Open for input. Default for {\ttfamily ifstream} and fstream.

Open for output. Default for {\ttfamily ofstream} and fstream.

Truncate an existing stream when opening. Default for {\ttfamily ofstream}.

This is an enumerated type.

{\ttfamily {\itshape \+\_\+\+Ios\+\_\+\+Seekdir} is} implementation-\/defined. Defined values of type seekdir are\+:
\begin{DoxyItemize}
\item beg
\item cur, equivalent to {\ttfamily SEEK\+\_\+\+CUR} in the C standard library.
\item end, equivalent to {\ttfamily SEEK\+\_\+\+END} in the C standard library.
\end{DoxyItemize}

Request a seek relative to the beginning of the stream.

Request a seek relative to the current position within the sequence.

Request a seek relative to the current end of the sequence.

The set of events that may be passed to an event callback.

erase\+\_\+event is used during \texorpdfstring{$\sim$}{\string~}ios() and copyfmt(). imbue\+\_\+event is used during imbue(). copyfmt\+\_\+event is used during copyfmt().

The type of an event callback function. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+e} & One of the members of the event enum. \\
\hline
{\em \+\_\+\+\_\+b} & Reference to the ios\+\_\+base object. \\
\hline
{\em \+\_\+\+\_\+i} & The integer provided when the callback was registered.\\
\hline
\end{DoxyParams}
Event callbacks are user defined functions that get called during several ios\+\_\+base and basic\+\_\+ios functions, specifically imbue(), copyfmt(), and \texorpdfstring{$\sim$}{\string~}ios().

Add the callback \+\_\+\+\_\+fn with parameter \+\_\+\+\_\+index. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+fn} & The function to add. \\
\hline
{\em \+\_\+\+\_\+index} & The integer to pass to the function when invoked.\\
\hline
\end{DoxyParams}
Registers a function as an event callback with an integer parameter to be passed to the function when invoked. Multiple copies of the function are allowed. If there are multiple callbacks, they are invoked in the order they were registered.

Access to format flags. \begin{DoxyReturn}{Returns}
The format control flags for both input and output.
\end{DoxyReturn}
Setting new format flags all at once. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+fmtfl} & The new flags to set. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The previous format control flags.
\end{DoxyReturn}
This function overwrites all the format flags with {\itshape \+\_\+\+\_\+fmtfl}.

Setting new format flags. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+fmtfl} & Additional flags to set. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The previous format control flags.
\end{DoxyReturn}
This function sets additional flags in format control. Flags that were previously set remain set.

Setting new format flags. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+fmtfl} & Additional flags to set. \\
\hline
{\em \+\_\+\+\_\+mask} & The flags mask for {\itshape fmtfl}. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The previous format control flags.
\end{DoxyReturn}
This function clears {\itshape mask} in the format flags, then sets {\itshape fmtfl} {\ttfamily \&} {\itshape mask}. An example mask is {\ttfamily ios\+\_\+base\+::adjustfield}.

Clearing format flags. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+mask} & The flags to unset.\\
\hline
\end{DoxyParams}
This function clears {\itshape \+\_\+\+\_\+mask} in the format flags.

Flags access. \begin{DoxyReturn}{Returns}
The precision to generate on certain output operations.
\end{DoxyReturn}
Be careful if you try to give a definition of {\itshape precision} here; see DR 189.

Changing flags. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+prec} & The new precision value. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The previous value of \doxylink{_classes_2lib_2raylib_2src_2external_2glad_8h_ae6165268264b393d031f89082216499d}{precision()}.
\end{DoxyReturn}
Flags access. \begin{DoxyReturn}{Returns}
The minimum field width to generate on output operations.
\end{DoxyReturn}
{\itshape Minimum field width} refers to the number of characters.

Changing flags. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+wide} & The new width value. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The previous value of \doxylink{_classes_2lib_2raylib_2src_2external_2_a_n_g_l_e_2_e_g_l_2eglext_8h_a3ce386e55bbeb900c1b23e064b89bd04}{width()}.
\end{DoxyReturn}
Interaction with the standard C I/O objects. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+sync} & Whether to synchronize or not. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
True if the standard streams were previously synchronized.
\end{DoxyReturn}
The synchronization referred to is {\itshape only} that between the standard C facilities (e.\+g., stdout) and the standard C++ objects (e.\+g., cout). User-\/declared streams are unaffected. See \href{https://gcc.gnu.org/onlinedocs/libstdc++/manual/fstreams.html\#std.io.filestreams.binary}{\texttt{ https\+://gcc.\+gnu.\+org/onlinedocs/libstdc++/manual/fstreams.\+html\#std.\+io.\+filestreams.\+binary}}

Setting a new locale. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+loc} & The new locale. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The previous locale.
\end{DoxyReturn}
Sets the new locale for this stream, and then invokes each callback with imbue\+\_\+event.

Locale access \begin{DoxyReturn}{Returns}
A copy of the current locale.
\end{DoxyReturn}
If {\ttfamily imbue(loc)} has previously been called, then this function returns {\ttfamily loc}. Otherwise, it returns a copy of {\ttfamily std\+::locale()}, the global C++ locale.

Locale access \begin{DoxyReturn}{Returns}
A reference to the current locale.
\end{DoxyReturn}
Like getloc above, but returns a reference instead of generating a copy.

Access to unique indices. \begin{DoxyReturn}{Returns}
An integer different from all previous calls.
\end{DoxyReturn}
This function returns a unique integer every time it is called. It can be used for any purpose, but is primarily intended to be a unique index for the iword and pword functions. The expectation is that an application calls xalloc in order to obtain an index in the iword and pword arrays that can be used without fear of conflict.

The implementation maintains a static variable that is incremented and returned on each invocation. xalloc is guaranteed to return an index that is safe to use in the iword and pword arrays.

Access to integer array. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+ix} & Index into the array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
A reference to an integer associated with the index.
\end{DoxyReturn}
The iword function provides access to an array of integers that can be used for any purpose. The array grows as required to hold the supplied index. All integers in the array are initialized to 0.

The implementation reserves several indices. You should use xalloc to obtain an index that is safe to use. Also note that since the array can grow dynamically, it is not safe to hold onto the reference.

Access to void pointer array. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+ix} & Index into the array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
A reference to a void\texorpdfstring{$\ast$}{*} associated with the index.
\end{DoxyReturn}
The pword function provides access to an array of pointers that can be used for any purpose. The array grows as required to hold the supplied index. All pointers in the array are initialized to 0.

The implementation reserves several indices. You should use xalloc to obtain an index that is safe to use. Also note that since the array can grow dynamically, it is not safe to hold onto the reference.

Invokes each callback with erase\+\_\+event. Destroys local storage.

Note that the ios\+\_\+base object for the standard streams never gets destroyed. As a result, any callbacks registered with the standard streams will not get invoked with erase\+\_\+event (unless copyfmt is used).

Calls base.\+setf(ios\+\_\+base\+::boolalpha).

Calls base.\+unsetf(ios\+\_\+base\+::boolalpha).

Calls base.\+setf(ios\+\_\+base\+::showbase).

Calls base.\+unsetf(ios\+\_\+base\+::showbase).

Calls base.\+setf(ios\+\_\+base\+::showpoint).

Calls base.\+unsetf(ios\+\_\+base\+::showpoint).

Calls base.\+setf(ios\+\_\+base\+::showpos).

Calls base.\+unsetf(ios\+\_\+base\+::showpos).

Calls base.\+setf(ios\+\_\+base\+::skipws).

Calls base.\+unsetf(ios\+\_\+base\+::skipws).

Calls base.\+setf(ios\+\_\+base\+::uppercase).

Calls base.\+unsetf(ios\+\_\+base\+::uppercase).

Calls base.\+setf(ios\+\_\+base\+::unitbuf).

Calls base.\+unsetf(ios\+\_\+base\+::unitbuf).

Calls base.\+setf(ios\+\_\+base\+::internal, ios\+\_\+base\+::adjustfield).

Calls base.\+setf(ios\+\_\+base\+::left, ios\+\_\+base\+::adjustfield).

Calls base.\+setf(ios\+\_\+base\+::right, ios\+\_\+base\+::adjustfield).

Calls base.\+setf(ios\+\_\+base\+::dec, ios\+\_\+base\+::basefield).

Calls base.\+setf(ios\+\_\+base\+::hex, ios\+\_\+base\+::basefield).

Calls base.\+setf(ios\+\_\+base\+::oct, ios\+\_\+base\+::basefield).

Calls base.\+setf(ios\+\_\+base\+::fixed, ios\+\_\+base\+::floatfield).

Calls base.\+setf(ios\+\_\+base\+::scientific, ios\+\_\+base\+::floatfield).

Exception possibly thrown by {\ttfamily shared\+\_\+ptr}.

Specialization using EBO.

Specialization not using EBO.

Return the stored pointer.

Return true if the stored pointer is not null.

Return true if use\+\_\+count() == 1.

If \texorpdfstring{$\ast$}{*}this owns a pointer, return the number of owners, otherwise zero.

Exchange both the owned pointer and the stored pointer.

Define an ordering based on ownership.

This function defines a strict weak ordering between two shared\+\_\+ptr or weak\+\_\+ptr objects, such that one object is less than the other unless they share ownership of the same pointer, or are both empty.

Thrown by utilities for testing exception safety.

Base class for checking address and label information about allocations. Create a std\+::map between the allocated address (void\texorpdfstring{$\ast$}{*}) and a datum for annotations, which are a pair of numbers corresponding to label and allocated size.

Base struct for condition policy.

Requires a public member function with the signature void throw\+\_\+conditionally()

Base class for incremental control and throw.

Never enter the condition.

Always enter the condition.

Enter the nth condition.

Class with exception generation control. Intended to be used as a value\+\_\+type in templatized code.

Note\+: Destructor not allowed to throw.

Type throwing via limit condition.

Allocator class with logging and exception generation control. Intended to be used as an allocator\+\_\+type in templatized code.

Note\+: Deallocate not allowed to throw.

Allocator throwing via limit condition.

Exception possibly thrown by {\ttfamily shared\+\_\+ptr}.

Exception possibly thrown by {\ttfamily shared\+\_\+ptr}.

Specialization using EBO.

Specialization not using EBO.

Return the stored pointer.

Return true if the stored pointer is not null.

Return true if use\+\_\+count() == 1.

If \texorpdfstring{$\ast$}{*}this owns a pointer, return the number of owners, otherwise zero.

Exchange both the owned pointer and the stored pointer.

Define an ordering based on ownership.

This function defines a strict weak ordering between two shared\+\_\+ptr or weak\+\_\+ptr objects, such that one object is less than the other unless they share ownership of the same pointer, or are both empty.

Thrown by utilities for testing exception safety.

Base class for checking address and label information about allocations. Create a std\+::map between the allocated address (void\texorpdfstring{$\ast$}{*}) and a datum for annotations, which are a pair of numbers corresponding to label and allocated size.

Base struct for condition policy.

Requires a public member function with the signature void throw\+\_\+conditionally()

Base class for incremental control and throw.

Never enter the condition.

Always enter the condition.

Enter the nth condition.

Class with exception generation control. Intended to be used as a value\+\_\+type in templatized code.

Note\+: Destructor not allowed to throw.

Type throwing via limit condition.

Allocator class with logging and exception generation control. Intended to be used as an allocator\+\_\+type in templatized code.

Note\+: Deallocate not allowed to throw.

Allocator throwing via limit condition.

Exception possibly thrown by {\ttfamily shared\+\_\+ptr}.

Exception possibly thrown by {\ttfamily shared\+\_\+ptr}.

Specialization using EBO.

Specialization not using EBO.

Return the stored pointer.

Return true if the stored pointer is not null.

Return true if use\+\_\+count() == 1.

If \texorpdfstring{$\ast$}{*}this owns a pointer, return the number of owners, otherwise zero.

Exchange both the owned pointer and the stored pointer.

Define an ordering based on ownership.

This function defines a strict weak ordering between two shared\+\_\+ptr or weak\+\_\+ptr objects, such that one object is less than the other unless they share ownership of the same pointer, or are both empty.

Thrown by utilities for testing exception safety.

Base class for checking address and label information about allocations. Create a std\+::map between the allocated address (void\texorpdfstring{$\ast$}{*}) and a datum for annotations, which are a pair of numbers corresponding to label and allocated size.

Base struct for condition policy.

Requires a public member function with the signature void throw\+\_\+conditionally()

Base class for incremental control and throw.

Never enter the condition.

Always enter the condition.

Enter the nth condition.

Class with exception generation control. Intended to be used as a value\+\_\+type in templatized code.

Note\+: Destructor not allowed to throw.

Type throwing via limit condition.

Allocator class with logging and exception generation control. Intended to be used as an allocator\+\_\+type in templatized code.

Note\+: Deallocate not allowed to throw.

Allocator throwing via limit condition.

Exception possibly thrown by {\ttfamily shared\+\_\+ptr}.Here is the call graph for this function\+:
% FIG 0
\Hypertarget{group__exceptions_gafcca609a0e064086e3d37ff25c85ab69}\label{group__exceptions_gafcca609a0e064086e3d37ff25c85ab69} 
\index{Exceptions@{Exceptions}!current\_exception@{current\_exception}}
\index{current\_exception@{current\_exception}!Exceptions@{Exceptions}}
\doxysubsubsection{\texorpdfstring{current\_exception()}{current\_exception()}}
{\footnotesize\ttfamily \mbox{\hyperlink{classstd_1_1____exception__ptr_1_1exception__ptr}{exception\+\_\+ptr}} std\+::current\+\_\+exception (\begin{DoxyParamCaption}{ }\end{DoxyParamCaption})}

Obtain an exception\+\_\+ptr to the currently handled exception. If there is none, or the currently handled exception is foreign, return the null value. \Hypertarget{group__exceptions_ga90822fbbd4a65b89480fb64d67fc69b6}\label{group__exceptions_ga90822fbbd4a65b89480fb64d67fc69b6} 
\index{Exceptions@{Exceptions}!make\_exception\_ptr@{make\_exception\_ptr}}
\index{make\_exception\_ptr@{make\_exception\_ptr}!Exceptions@{Exceptions}}
\doxysubsubsection{\texorpdfstring{make\_exception\_ptr()}{make\_exception\_ptr()}}
{\footnotesize\ttfamily template$<$typename \+\_\+\+Ex $>$ \\
\mbox{\hyperlink{classstd_1_1____exception__ptr_1_1exception__ptr}{exception\+\_\+ptr}} std\+::make\+\_\+exception\+\_\+ptr (\begin{DoxyParamCaption}\item[{\+\_\+\+Ex}]{ }\end{DoxyParamCaption})}

\Hypertarget{group__exceptions_ga64d0b68338d7edbfd7d95f4177dbc442}\label{group__exceptions_ga64d0b68338d7edbfd7d95f4177dbc442} 
\index{Exceptions@{Exceptions}!rethrow\_exception@{rethrow\_exception}}
\index{rethrow\_exception@{rethrow\_exception}!Exceptions@{Exceptions}}
\doxysubsubsection{\texorpdfstring{rethrow\_exception()}{rethrow\_exception()}}
{\footnotesize\ttfamily \mbox{\hyperlink{_classes_2lib_2raylib_2src_2external_2_a_n_g_l_e_2_e_g_l_2eglext_8h_a0f7193911e632cd84da4082d5c628fe3}{void}} std\+::rethrow\+\_\+exception (\begin{DoxyParamCaption}\item[{\mbox{\hyperlink{classstd_1_1____exception__ptr_1_1exception__ptr}{exception\+\_\+ptr}}}]{ }\end{DoxyParamCaption})}



Throw the object pointed to by the exception\+\_\+ptr. 

Here is the call graph for this function\+:
% FIG 1
Here is the caller graph for this function\+:
% FIG 2
