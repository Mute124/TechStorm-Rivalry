\doxysection{Locales}
\hypertarget{group__locales}{}\label{group__locales}\index{Locales@{Locales}}
\begin{DoxyCompactItemize}
\item 
namespace std \mbox{\hyperlink{group__locales_ga544d74b2d5d0dd2197f5fd6f02691de0}{\+\_\+\+GLIBCXX\+\_\+\+VISIBILITY}} (default)
\end{DoxyCompactItemize}


\doxysubsection{Detailed Description}
Classes and functions for internationalization and localization.

Classes and functions for internationalization and localization.

Classes and functions for internationalization and localization.

\doxysubsection{Function Documentation}
\Hypertarget{group__locales_ga544d74b2d5d0dd2197f5fd6f02691de0}\label{group__locales_ga544d74b2d5d0dd2197f5fd6f02691de0} 
\index{Locales@{Locales}!\_GLIBCXX\_VISIBILITY@{\_GLIBCXX\_VISIBILITY}}
\index{\_GLIBCXX\_VISIBILITY@{\_GLIBCXX\_VISIBILITY}!Locales@{Locales}}
\doxysubsubsection{\texorpdfstring{\_GLIBCXX\_VISIBILITY()}{\_GLIBCXX\_VISIBILITY()}}
{\footnotesize\ttfamily namespace std \+\_\+\+GLIBCXX\+\_\+\+VISIBILITY (\begin{DoxyParamCaption}\item[{default}]{ }\end{DoxyParamCaption})}

Empty base class for codecvt facet \mbox{[}22.\+2.\+1.\+5\mbox{]}.

Common base for codecvt functions.

This template class provides implementations of the public functions that forward to the protected virtual functions.

This template also provides abstract stubs for the protected virtual functions.

Convert from internal to external character set.

Converts input string of intern\+\_\+type to output string of extern\+\_\+type. This is analogous to wcsrtombs. It does this by calling codecvt\+::do\+\_\+out.

The source and destination character sets are determined by the facet\textquotesingle{}s locale, internal and external types.

The characters in \mbox{[}from,from\+\_\+end) are converted and written to \mbox{[}to,to\+\_\+end). from\+\_\+next and to\+\_\+next are set to point to the character following the last successfully converted character, respectively. If the result needed no conversion, from\+\_\+next and to\+\_\+next are not affected.

The {\itshape state} argument should be initialized if the input is at the beginning and carried from a previous call if continuing conversion. There are no guarantees about how {\itshape state} is used.

The result returned is a member of codecvt\+\_\+base\+::result. If all the input is converted, returns codecvt\+\_\+base\+::ok. If no conversion is necessary, returns codecvt\+\_\+base\+::noconv. If the input ends early or there is insufficient space in the output, returns codecvt\+\_\+base\+::partial. Otherwise the conversion failed and codecvt\+\_\+base\+::error is returned.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+state} & Persistent conversion state data. \\
\hline
{\em \+\_\+\+\_\+from} & Start of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+end} & End of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+next} & Returns start of unconverted data. \\
\hline
{\em \+\_\+\+\_\+to} & Start of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+end} & End of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+next} & Returns start of unused output area. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
codecvt\+\_\+base\+::result.
\end{DoxyReturn}
Reset conversion state.

Writes characters to output that would restore {\itshape state} to initial conditions. The idea is that if a partial conversion occurs, then the converting the characters written by this function would leave the state in initial conditions, rather than partial conversion state. It does this by calling codecvt\+::do\+\_\+unshift().

For example, if 4 external characters always converted to 1 internal character, and input to \doxylink{_classes_2lib_2raylib_2src_2external_2glad_8h_a83ad0ee7f1e06b59c90271716e689080}{in()} had 6 external characters with state saved, this function would write two characters to the output and set the state to initialized conditions.

The source and destination character sets are determined by the facet\textquotesingle{}s locale, internal and external types.

The result returned is a member of codecvt\+\_\+base\+::result. If the state could be reset and data written, returns codecvt\+\_\+base\+::ok. If no conversion is necessary, returns codecvt\+\_\+base\+::noconv. If the output has insufficient space, returns codecvt\+\_\+base\+::partial. Otherwise the reset failed and codecvt\+\_\+base\+::error is returned.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+state} & Persistent conversion state data. \\
\hline
{\em \+\_\+\+\_\+to} & Start of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+end} & End of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+next} & Returns start of unused output area. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
codecvt\+\_\+base\+::result.
\end{DoxyReturn}
Convert from external to internal character set.

Converts input string of extern\+\_\+type to output string of intern\+\_\+type. This is analogous to mbsrtowcs. It does this by calling codecvt\+::do\+\_\+in.

The source and destination character sets are determined by the facet\textquotesingle{}s locale, internal and external types.

The characters in \mbox{[}from,from\+\_\+end) are converted and written to \mbox{[}to,to\+\_\+end). from\+\_\+next and to\+\_\+next are set to point to the character following the last successfully converted character, respectively. If the result needed no conversion, from\+\_\+next and to\+\_\+next are not affected.

The {\itshape state} argument should be initialized if the input is at the beginning and carried from a previous call if continuing conversion. There are no guarantees about how {\itshape state} is used.

The result returned is a member of codecvt\+\_\+base\+::result. If all the input is converted, returns codecvt\+\_\+base\+::ok. If no conversion is necessary, returns codecvt\+\_\+base\+::noconv. If the input ends early or there is insufficient space in the output, returns codecvt\+\_\+base\+::partial. Otherwise the conversion failed and codecvt\+\_\+base\+::error is returned.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+state} & Persistent conversion state data. \\
\hline
{\em \+\_\+\+\_\+from} & Start of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+end} & End of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+next} & Returns start of unconverted data. \\
\hline
{\em \+\_\+\+\_\+to} & Start of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+end} & End of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+next} & Returns start of unused output area. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
codecvt\+\_\+base\+::result.
\end{DoxyReturn}
Convert from internal to external character set.

Converts input string of intern\+\_\+type to output string of extern\+\_\+type. This function is a hook for derived classes to change the value returned. \begin{DoxySeeAlso}{See also}
out for more information.
\end{DoxySeeAlso}
Primary class template codecvt.

NB\+: Generic, mostly useless implementation.

class codecvt$<$char, char, mbstate\+\_\+t$>$ specialization.

class codecvt\+\_\+byname \mbox{[}22.\+2.\+1.\+6\mbox{]}.

Container class for localization functionality.

The locale class is first a class wrapper for C library locales. It is also an extensible container for user-\/defined localization. A locale is a collection of facets that implement various localization features such as money, time, and number printing.

Constructing C++ locales does not change the C library locale.

This library supports efficient construction and copying of locales through a reference counting implementation of the locale class.

Definition of locale\+::category.

Category values.

The standard category values are none, ctype, numeric, collate, time, monetary, and messages. They form a bitmask that supports union and intersection. The category all is the union of these values.

NB\+: Order must match \+\_\+\+S\+\_\+facet\+\_\+categories definition in locale.\+cc

Common base for ctype facet

This template class provides implementations of the public functions that forward to the protected virtual functions.

This template also provides abstract stubs for the protected virtual functions.

Typedef for the template parameter

Test char\+\_\+type classification.

This function finds a mask M for {\itshape \+\_\+\+\_\+c} and compares it to mask {\itshape \+\_\+\+\_\+m}. It does so by returning the value of ctype$<$char\+\_\+type$>$\+::do\+\_\+is().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to compare the mask of. \\
\hline
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
(M \& \+\_\+\+\_\+m) != 0.
\end{DoxyReturn}
Return a mask array.

This function finds the mask for each char\+\_\+type in the range \mbox{[}lo,hi) and successively writes it to vec. vec must have as many elements as the char array. It does so by returning the value of ctype$<$char\+\_\+type$>$\+::do\+\_\+is().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+vec} & Pointer to an array of mask storage. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type matching a mask

This function searches for and returns the first char\+\_\+type c in \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,c)} is true. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+scan\+\_\+is().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to matching char\+\_\+type if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type not matching a mask

This function searches for and returns the first char\+\_\+type c in \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,c)} is false. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+scan\+\_\+not().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to non-\/matching char if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to uppercase.

This function converts the argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+toupper().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This function converts each char\+\_\+type in the range \mbox{[}lo,hi) to uppercase if possible. Other elements remain untouched. It does so by returning ctype$<$char\+\_\+type$>$\+:: do\+\_\+toupper(lo, hi).


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This function converts the argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+tolower(c).


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to lowercase if possible. Other elements remain untouched. It does so by returning ctype$<$char\+\_\+type$>$\+:: do\+\_\+tolower(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi).


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char to char\+\_\+type

This function converts the char argument to char\+\_\+type using the simplest reasonable transformation. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+widen(c).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char\+\_\+type.
\end{DoxyReturn}
Widen array to char\+\_\+type

This function converts each char in the input to char\+\_\+type using the simplest reasonable transformation. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+widen(c).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char\+\_\+type to char

This function converts the char\+\_\+type to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+narrow(\+\_\+\+\_\+c).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char.
\end{DoxyReturn}
Narrow array to char array

This function converts each char\+\_\+type in the input to char using the simplest reasonable transformation and writes the results to the destination array. For any char\+\_\+type in the input that cannot be converted, {\itshape dfault} is used instead. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+narrow(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi, \+\_\+\+\_\+dfault, \+\_\+\+\_\+to).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Test char\+\_\+type classification.

This function finds a mask M for {\itshape c} and compares it to mask {\itshape m}.

do\+\_\+is() is a hook for a derived facet to change the behavior of classifying. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to find the mask of. \\
\hline
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
(M \& \+\_\+\+\_\+m) != 0.
\end{DoxyReturn}
Return a mask array.

This function finds the mask for each char\+\_\+type in the range \mbox{[}lo,hi) and successively writes it to vec. vec must have as many elements as the input.

do\+\_\+is() is a hook for a derived facet to change the behavior of classifying. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+vec} & Pointer to an array of mask storage. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type matching mask

This function searches for and returns the first char\+\_\+type c in \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(\+\_\+\+\_\+m,c)} is true.

do\+\_\+scan\+\_\+is() is a hook for a derived facet to change the behavior of match searching. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a matching char\+\_\+type if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type not matching mask

This function searches for and returns a pointer to the first char\+\_\+type c of \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,c)} is false.

do\+\_\+scan\+\_\+is() is a hook for a derived facet to change the behavior of match searching. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a non-\/matching char\+\_\+type if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to uppercase.

This virtual function converts the char\+\_\+type argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This virtual function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to uppercase if possible. Other elements remain untouched.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This virtual function converts the argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This virtual function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to lowercase if possible. Other elements remain untouched.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char

This virtual function converts the char to char\+\_\+type using the simplest reasonable transformation.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char\+\_\+type
\end{DoxyReturn}
Widen char array

This function converts each char in the input to char\+\_\+type using the simplest reasonable transformation.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char\+\_\+type to char

This virtual function converts the argument to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char.
\end{DoxyReturn}
Narrow char\+\_\+type array to char

This virtual function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to char using the simplest reasonable transformation and writes the results to the destination array. For any element in the input that cannot be converted, {\itshape \+\_\+\+\_\+dfault} is used instead.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Primary class template ctype facet.

This template class defines classification and conversion functions for character sets. It wraps cctype functionality. Ctype gets used by streams for many I/O operations.

This template provides the protected virtual functions the developer will have to replace in a derived class or specialization to make a working facet. The public functions that access them are defined in \+\_\+\+\_\+ctype\+\_\+abstract\+\_\+base, to allow for implementation flexibility. See ctype$<$wchar\+\_\+t$>$ for an example. The functions are documented in \+\_\+\+\_\+ctype\+\_\+abstract\+\_\+base.

Note\+: implementations are provided for all the protected virtual functions, but will likely not be useful.

The facet id for ctype$<$char\+\_\+type$>$

The ctype$<$char$>$ specialization.

This class defines classification and conversion functions for the char type. It gets used by char streams for many I/O operations. The char specialization provides a number of optimizations as well.

Typedef for the template parameter char.

The facet id for ctype$<$char$>$

The size of the mask table. It is SCHAR\+\_\+\+MAX + 1.

Constructor performs initialization.

This is the constructor provided by the standard.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+table} & If non-\/zero, table is used as the per-\/char mask. Else classic\+\_\+table() is used. \\
\hline
{\em \+\_\+\+\_\+del} & If true, passes ownership of table to this facet. \\
\hline
{\em \+\_\+\+\_\+refs} & Passed to the base facet class.\\
\hline
\end{DoxyParams}
Constructor performs static initialization.

This constructor is used to construct the initial C locale facet.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+cloc} & Handle to C locale data. \\
\hline
{\em \+\_\+\+\_\+table} & If non-\/zero, table is used as the per-\/char mask. \\
\hline
{\em \+\_\+\+\_\+del} & If true, passes ownership of table to this facet. \\
\hline
{\em \+\_\+\+\_\+refs} & Passed to the base facet class.\\
\hline
\end{DoxyParams}
Test char classification.

This function compares the mask table\mbox{[}c\mbox{]} to {\itshape \+\_\+\+\_\+m}.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to compare the mask of. \\
\hline
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
True if \+\_\+\+\_\+m \& table\mbox{[}\+\_\+\+\_\+c\mbox{]} is true, false otherwise.
\end{DoxyReturn}
Return a mask array.

This function finds the mask for each char in the range \mbox{[}lo, hi) and successively writes it to vec. vec must have as many elements as the char array.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+vec} & Pointer to an array of mask storage. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char matching a mask

This function searches for and returns the first char in \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,char)} is true.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a matching char if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char not matching a mask

This function searches for and returns a pointer to the first char in \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,char)} is false.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a non-\/matching char if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to uppercase.

This function converts the char argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

toupper() acts as if it returns ctype$<$char$>$\+::do\+\_\+toupper(c). do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This function converts each char in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to uppercase if possible. Other chars remain untouched.

toupper() acts as if it returns ctype$<$char$>$\+:: do\+\_\+toupper(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi). do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This function converts the char argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

tolower() acts as if it returns ctype$<$char$>$\+::do\+\_\+tolower(\+\_\+\+\_\+c). do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This function converts each char in the range \mbox{[}lo,hi) to lowercase if possible. Other chars remain untouched.

tolower() acts as if it returns ctype$<$char$>$\+:: do\+\_\+tolower(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi). do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char

This function converts the char to char\+\_\+type using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be returned unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+widen(c). do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted character.
\end{DoxyReturn}
Widen char array

This function converts each char in the input to char using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+widen(c). do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char

This function converts the char to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead. For an underived ctype$<$char$>$ facet, {\itshape c} will be returned unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+narrow(c). do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted character.
\end{DoxyReturn}
Narrow char array

This function converts each char in the input to char using the simplest reasonable transformation and writes the results to the destination array. For any char in the input that cannot be converted, {\itshape dfault} is used instead. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+narrow(lo, hi, dfault, to). do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Returns a pointer to the mask table provided to the constructor, or the default from classic\+\_\+table() if none was provided.

Returns a pointer to the C locale mask table.

Destructor.

This function deletes \doxylink{classtable}{table()} if {\itshape del} was true in the constructor.

Convert to uppercase.

This virtual function converts the char argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This virtual function converts each char in the range \mbox{[}lo,hi) to uppercase if possible. Other chars remain untouched.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This virtual function converts the char argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This virtual function converts each char in the range \mbox{[}lo,hi) to lowercase if possible. Other chars remain untouched.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char

This virtual function converts the char to char using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be returned unchanged.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted character.
\end{DoxyReturn}
Widen char array

This function converts each char in the range \mbox{[}lo,hi) to char using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char

This virtual function converts the char to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead. For an underived ctype$<$char$>$ facet, {\itshape c} will be returned unchanged.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char.
\end{DoxyReturn}
Narrow char array to char array

This virtual function converts each char in the range \mbox{[}lo,hi) to char using the simplest reasonable transformation and writes the results to the destination array. For any char in the input that cannot be converted, {\itshape dfault} is used instead. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
class ctype\+\_\+byname \mbox{[}22.\+2.\+1.\+2\mbox{]}.

22.\+2.\+1.\+4 Class ctype\+\_\+byname specializations.

Primary class template numpunct.

This facet stores several pieces of information related to printing and scanning numbers, such as the decimal point character. It takes a template parameter specifying the char type. The numpunct facet is used by streams for many I/O operations involving numbers.

The numpunct template uses protected virtual functions to provide the actual results. The public accessors forward the call to the virtual functions. These virtual functions are hooks for developers to implement the behavior they require from a numpunct facet.

Public typedefs

Time format ordering data.

This class provides an enum representing different orderings of time\+: day, month, and year.

Numpunct facet id.

Internal constructor. Not for general use.

This is a constructor for use by the library itself to set up new locales.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+cloc} & The C locale. \\
\hline
{\em \+\_\+\+\_\+s} & The name of a locale. \\
\hline
{\em refs} & Passed to the base facet class.\\
\hline
\end{DoxyParams}
Primary class template time\+\_\+get.

This facet encapsulates the code to parse and return a date or time from a string. It is used by the istream numeric extraction operators.

The time\+\_\+get template uses protected virtual functions to provide the actual results. The public accessors forward the call to the virtual functions. These virtual functions are hooks for developers to implement the behavior they require from the time\+\_\+get facet.

Public typedefs

Empty base class for codecvt facet \mbox{[}22.\+2.\+1.\+5\mbox{]}.

Common base for codecvt functions.

This template class provides implementations of the public functions that forward to the protected virtual functions.

This template also provides abstract stubs for the protected virtual functions.

Convert from internal to external character set.

Converts input string of intern\+\_\+type to output string of extern\+\_\+type. This is analogous to wcsrtombs. It does this by calling codecvt\+::do\+\_\+out.

The source and destination character sets are determined by the facet\textquotesingle{}s locale, internal and external types.

The characters in \mbox{[}from,from\+\_\+end) are converted and written to \mbox{[}to,to\+\_\+end). from\+\_\+next and to\+\_\+next are set to point to the character following the last successfully converted character, respectively. If the result needed no conversion, from\+\_\+next and to\+\_\+next are not affected.

The {\itshape state} argument should be initialized if the input is at the beginning and carried from a previous call if continuing conversion. There are no guarantees about how {\itshape state} is used.

The result returned is a member of codecvt\+\_\+base\+::result. If all the input is converted, returns codecvt\+\_\+base\+::ok. If no conversion is necessary, returns codecvt\+\_\+base\+::noconv. If the input ends early or there is insufficient space in the output, returns codecvt\+\_\+base\+::partial. Otherwise the conversion failed and codecvt\+\_\+base\+::error is returned.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+state} & Persistent conversion state data. \\
\hline
{\em \+\_\+\+\_\+from} & Start of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+end} & End of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+next} & Returns start of unconverted data. \\
\hline
{\em \+\_\+\+\_\+to} & Start of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+end} & End of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+next} & Returns start of unused output area. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
codecvt\+\_\+base\+::result.
\end{DoxyReturn}
Reset conversion state.

Writes characters to output that would restore {\itshape state} to initial conditions. The idea is that if a partial conversion occurs, then the converting the characters written by this function would leave the state in initial conditions, rather than partial conversion state. It does this by calling codecvt\+::do\+\_\+unshift().

For example, if 4 external characters always converted to 1 internal character, and input to \doxylink{_classes_2lib_2raylib_2src_2external_2glad_8h_a83ad0ee7f1e06b59c90271716e689080}{in()} had 6 external characters with state saved, this function would write two characters to the output and set the state to initialized conditions.

The source and destination character sets are determined by the facet\textquotesingle{}s locale, internal and external types.

The result returned is a member of codecvt\+\_\+base\+::result. If the state could be reset and data written, returns codecvt\+\_\+base\+::ok. If no conversion is necessary, returns codecvt\+\_\+base\+::noconv. If the output has insufficient space, returns codecvt\+\_\+base\+::partial. Otherwise the reset failed and codecvt\+\_\+base\+::error is returned.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+state} & Persistent conversion state data. \\
\hline
{\em \+\_\+\+\_\+to} & Start of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+end} & End of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+next} & Returns start of unused output area. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
codecvt\+\_\+base\+::result.
\end{DoxyReturn}
Convert from external to internal character set.

Converts input string of extern\+\_\+type to output string of intern\+\_\+type. This is analogous to mbsrtowcs. It does this by calling codecvt\+::do\+\_\+in.

The source and destination character sets are determined by the facet\textquotesingle{}s locale, internal and external types.

The characters in \mbox{[}from,from\+\_\+end) are converted and written to \mbox{[}to,to\+\_\+end). from\+\_\+next and to\+\_\+next are set to point to the character following the last successfully converted character, respectively. If the result needed no conversion, from\+\_\+next and to\+\_\+next are not affected.

The {\itshape state} argument should be initialized if the input is at the beginning and carried from a previous call if continuing conversion. There are no guarantees about how {\itshape state} is used.

The result returned is a member of codecvt\+\_\+base\+::result. If all the input is converted, returns codecvt\+\_\+base\+::ok. If no conversion is necessary, returns codecvt\+\_\+base\+::noconv. If the input ends early or there is insufficient space in the output, returns codecvt\+\_\+base\+::partial. Otherwise the conversion failed and codecvt\+\_\+base\+::error is returned.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+state} & Persistent conversion state data. \\
\hline
{\em \+\_\+\+\_\+from} & Start of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+end} & End of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+next} & Returns start of unconverted data. \\
\hline
{\em \+\_\+\+\_\+to} & Start of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+end} & End of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+next} & Returns start of unused output area. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
codecvt\+\_\+base\+::result.
\end{DoxyReturn}
Convert from internal to external character set.

Converts input string of intern\+\_\+type to output string of extern\+\_\+type. This function is a hook for derived classes to change the value returned. \begin{DoxySeeAlso}{See also}
out for more information.
\end{DoxySeeAlso}
Primary class template codecvt.

NB\+: Generic, mostly useless implementation.

class codecvt$<$char, char, mbstate\+\_\+t$>$ specialization.

class codecvt\+\_\+byname \mbox{[}22.\+2.\+1.\+6\mbox{]}.

Container class for localization functionality.

The locale class is first a class wrapper for C library locales. It is also an extensible container for user-\/defined localization. A locale is a collection of facets that implement various localization features such as money, time, and number printing.

Constructing C++ locales does not change the C library locale.

This library supports efficient construction and copying of locales through a reference counting implementation of the locale class.

Definition of locale\+::category.

Category values.

The standard category values are none, ctype, numeric, collate, time, monetary, and messages. They form a bitmask that supports union and intersection. The category all is the union of these values.

NB\+: Order must match \+\_\+\+S\+\_\+facet\+\_\+categories definition in locale.\+cc

Common base for ctype facet

This template class provides implementations of the public functions that forward to the protected virtual functions.

This template also provides abstract stubs for the protected virtual functions.

Typedef for the template parameter

Test char\+\_\+type classification.

This function finds a mask M for {\itshape \+\_\+\+\_\+c} and compares it to mask {\itshape \+\_\+\+\_\+m}. It does so by returning the value of ctype$<$char\+\_\+type$>$\+::do\+\_\+is().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to compare the mask of. \\
\hline
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
(M \& \+\_\+\+\_\+m) != 0.
\end{DoxyReturn}
Return a mask array.

This function finds the mask for each char\+\_\+type in the range \mbox{[}lo,hi) and successively writes it to vec. vec must have as many elements as the char array. It does so by returning the value of ctype$<$char\+\_\+type$>$\+::do\+\_\+is().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+vec} & Pointer to an array of mask storage. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type matching a mask

This function searches for and returns the first char\+\_\+type c in \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,c)} is true. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+scan\+\_\+is().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to matching char\+\_\+type if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type not matching a mask

This function searches for and returns the first char\+\_\+type c in \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,c)} is false. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+scan\+\_\+not().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to non-\/matching char if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to uppercase.

This function converts the argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+toupper().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This function converts each char\+\_\+type in the range \mbox{[}lo,hi) to uppercase if possible. Other elements remain untouched. It does so by returning ctype$<$char\+\_\+type$>$\+:: do\+\_\+toupper(lo, hi).


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This function converts the argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+tolower(c).


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to lowercase if possible. Other elements remain untouched. It does so by returning ctype$<$char\+\_\+type$>$\+:: do\+\_\+tolower(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi).


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char to char\+\_\+type

This function converts the char argument to char\+\_\+type using the simplest reasonable transformation. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+widen(c).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char\+\_\+type.
\end{DoxyReturn}
Widen array to char\+\_\+type

This function converts each char in the input to char\+\_\+type using the simplest reasonable transformation. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+widen(c).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char\+\_\+type to char

This function converts the char\+\_\+type to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+narrow(\+\_\+\+\_\+c).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char.
\end{DoxyReturn}
Narrow array to char array

This function converts each char\+\_\+type in the input to char using the simplest reasonable transformation and writes the results to the destination array. For any char\+\_\+type in the input that cannot be converted, {\itshape dfault} is used instead. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+narrow(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi, \+\_\+\+\_\+dfault, \+\_\+\+\_\+to).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Test char\+\_\+type classification.

This function finds a mask M for {\itshape c} and compares it to mask {\itshape m}.

do\+\_\+is() is a hook for a derived facet to change the behavior of classifying. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to find the mask of. \\
\hline
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
(M \& \+\_\+\+\_\+m) != 0.
\end{DoxyReturn}
Return a mask array.

This function finds the mask for each char\+\_\+type in the range \mbox{[}lo,hi) and successively writes it to vec. vec must have as many elements as the input.

do\+\_\+is() is a hook for a derived facet to change the behavior of classifying. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+vec} & Pointer to an array of mask storage. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type matching mask

This function searches for and returns the first char\+\_\+type c in \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(\+\_\+\+\_\+m,c)} is true.

do\+\_\+scan\+\_\+is() is a hook for a derived facet to change the behavior of match searching. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a matching char\+\_\+type if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type not matching mask

This function searches for and returns a pointer to the first char\+\_\+type c of \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,c)} is false.

do\+\_\+scan\+\_\+is() is a hook for a derived facet to change the behavior of match searching. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a non-\/matching char\+\_\+type if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to uppercase.

This virtual function converts the char\+\_\+type argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This virtual function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to uppercase if possible. Other elements remain untouched.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This virtual function converts the argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This virtual function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to lowercase if possible. Other elements remain untouched.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char

This virtual function converts the char to char\+\_\+type using the simplest reasonable transformation.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char\+\_\+type
\end{DoxyReturn}
Widen char array

This function converts each char in the input to char\+\_\+type using the simplest reasonable transformation.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char\+\_\+type to char

This virtual function converts the argument to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char.
\end{DoxyReturn}
Narrow char\+\_\+type array to char

This virtual function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to char using the simplest reasonable transformation and writes the results to the destination array. For any element in the input that cannot be converted, {\itshape \+\_\+\+\_\+dfault} is used instead.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Primary class template ctype facet.

This template class defines classification and conversion functions for character sets. It wraps cctype functionality. Ctype gets used by streams for many I/O operations.

This template provides the protected virtual functions the developer will have to replace in a derived class or specialization to make a working facet. The public functions that access them are defined in \+\_\+\+\_\+ctype\+\_\+abstract\+\_\+base, to allow for implementation flexibility. See ctype$<$wchar\+\_\+t$>$ for an example. The functions are documented in \+\_\+\+\_\+ctype\+\_\+abstract\+\_\+base.

Note\+: implementations are provided for all the protected virtual functions, but will likely not be useful.

The facet id for ctype$<$char\+\_\+type$>$

The ctype$<$char$>$ specialization.

This class defines classification and conversion functions for the char type. It gets used by char streams for many I/O operations. The char specialization provides a number of optimizations as well.

Typedef for the template parameter char.

The facet id for ctype$<$char$>$

The size of the mask table. It is SCHAR\+\_\+\+MAX + 1.

Constructor performs initialization.

This is the constructor provided by the standard.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+table} & If non-\/zero, table is used as the per-\/char mask. Else classic\+\_\+table() is used. \\
\hline
{\em \+\_\+\+\_\+del} & If true, passes ownership of table to this facet. \\
\hline
{\em \+\_\+\+\_\+refs} & Passed to the base facet class.\\
\hline
\end{DoxyParams}
Constructor performs static initialization.

This constructor is used to construct the initial C locale facet.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+cloc} & Handle to C locale data. \\
\hline
{\em \+\_\+\+\_\+table} & If non-\/zero, table is used as the per-\/char mask. \\
\hline
{\em \+\_\+\+\_\+del} & If true, passes ownership of table to this facet. \\
\hline
{\em \+\_\+\+\_\+refs} & Passed to the base facet class.\\
\hline
\end{DoxyParams}
Test char classification.

This function compares the mask table\mbox{[}c\mbox{]} to {\itshape \+\_\+\+\_\+m}.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to compare the mask of. \\
\hline
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
True if \+\_\+\+\_\+m \& table\mbox{[}\+\_\+\+\_\+c\mbox{]} is true, false otherwise.
\end{DoxyReturn}
Return a mask array.

This function finds the mask for each char in the range \mbox{[}lo, hi) and successively writes it to vec. vec must have as many elements as the char array.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+vec} & Pointer to an array of mask storage. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char matching a mask

This function searches for and returns the first char in \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,char)} is true.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a matching char if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char not matching a mask

This function searches for and returns a pointer to the first char in \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,char)} is false.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a non-\/matching char if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to uppercase.

This function converts the char argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

toupper() acts as if it returns ctype$<$char$>$\+::do\+\_\+toupper(c). do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This function converts each char in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to uppercase if possible. Other chars remain untouched.

toupper() acts as if it returns ctype$<$char$>$\+:: do\+\_\+toupper(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi). do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This function converts the char argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

tolower() acts as if it returns ctype$<$char$>$\+::do\+\_\+tolower(\+\_\+\+\_\+c). do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This function converts each char in the range \mbox{[}lo,hi) to lowercase if possible. Other chars remain untouched.

tolower() acts as if it returns ctype$<$char$>$\+:: do\+\_\+tolower(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi). do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char

This function converts the char to char\+\_\+type using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be returned unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+widen(c). do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted character.
\end{DoxyReturn}
Widen char array

This function converts each char in the input to char using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+widen(c). do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char

This function converts the char to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead. For an underived ctype$<$char$>$ facet, {\itshape c} will be returned unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+narrow(c). do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted character.
\end{DoxyReturn}
Narrow char array

This function converts each char in the input to char using the simplest reasonable transformation and writes the results to the destination array. For any char in the input that cannot be converted, {\itshape dfault} is used instead. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+narrow(lo, hi, dfault, to). do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Returns a pointer to the mask table provided to the constructor, or the default from classic\+\_\+table() if none was provided.

Returns a pointer to the C locale mask table.

Destructor.

This function deletes \doxylink{classtable}{table()} if {\itshape del} was true in the constructor.

Convert to uppercase.

This virtual function converts the char argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This virtual function converts each char in the range \mbox{[}lo,hi) to uppercase if possible. Other chars remain untouched.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This virtual function converts the char argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This virtual function converts each char in the range \mbox{[}lo,hi) to lowercase if possible. Other chars remain untouched.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char

This virtual function converts the char to char using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be returned unchanged.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted character.
\end{DoxyReturn}
Widen char array

This function converts each char in the range \mbox{[}lo,hi) to char using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char

This virtual function converts the char to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead. For an underived ctype$<$char$>$ facet, {\itshape c} will be returned unchanged.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char.
\end{DoxyReturn}
Narrow char array to char array

This virtual function converts each char in the range \mbox{[}lo,hi) to char using the simplest reasonable transformation and writes the results to the destination array. For any char in the input that cannot be converted, {\itshape dfault} is used instead. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
class ctype\+\_\+byname \mbox{[}22.\+2.\+1.\+2\mbox{]}.

22.\+2.\+1.\+4 Class ctype\+\_\+byname specializations.

Primary class template numpunct.

This facet stores several pieces of information related to printing and scanning numbers, such as the decimal point character. It takes a template parameter specifying the char type. The numpunct facet is used by streams for many I/O operations involving numbers.

The numpunct template uses protected virtual functions to provide the actual results. The public accessors forward the call to the virtual functions. These virtual functions are hooks for developers to implement the behavior they require from a numpunct facet.

Public typedefs

Time format ordering data.

This class provides an enum representing different orderings of time\+: day, month, and year.

Numpunct facet id.

Internal constructor. Not for general use.

This is a constructor for use by the library itself to set up new locales.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+cloc} & The C locale. \\
\hline
{\em \+\_\+\+\_\+s} & The name of a locale. \\
\hline
{\em refs} & Passed to the base facet class.\\
\hline
\end{DoxyParams}
Primary class template time\+\_\+get.

This facet encapsulates the code to parse and return a date or time from a string. It is used by the istream numeric extraction operators.

The time\+\_\+get template uses protected virtual functions to provide the actual results. The public accessors forward the call to the virtual functions. These virtual functions are hooks for developers to implement the behavior they require from the time\+\_\+get facet.

Public typedefs

Empty base class for codecvt facet \mbox{[}22.\+2.\+1.\+5\mbox{]}.

Common base for codecvt functions.

This template class provides implementations of the public functions that forward to the protected virtual functions.

This template also provides abstract stubs for the protected virtual functions.

Convert from internal to external character set.

Converts input string of intern\+\_\+type to output string of extern\+\_\+type. This is analogous to wcsrtombs. It does this by calling codecvt\+::do\+\_\+out.

The source and destination character sets are determined by the facet\textquotesingle{}s locale, internal and external types.

The characters in \mbox{[}from,from\+\_\+end) are converted and written to \mbox{[}to,to\+\_\+end). from\+\_\+next and to\+\_\+next are set to point to the character following the last successfully converted character, respectively. If the result needed no conversion, from\+\_\+next and to\+\_\+next are not affected.

The {\itshape state} argument should be initialized if the input is at the beginning and carried from a previous call if continuing conversion. There are no guarantees about how {\itshape state} is used.

The result returned is a member of codecvt\+\_\+base\+::result. If all the input is converted, returns codecvt\+\_\+base\+::ok. If no conversion is necessary, returns codecvt\+\_\+base\+::noconv. If the input ends early or there is insufficient space in the output, returns codecvt\+\_\+base\+::partial. Otherwise the conversion failed and codecvt\+\_\+base\+::error is returned.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+state} & Persistent conversion state data. \\
\hline
{\em \+\_\+\+\_\+from} & Start of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+end} & End of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+next} & Returns start of unconverted data. \\
\hline
{\em \+\_\+\+\_\+to} & Start of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+end} & End of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+next} & Returns start of unused output area. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
codecvt\+\_\+base\+::result.
\end{DoxyReturn}
Reset conversion state.

Writes characters to output that would restore {\itshape state} to initial conditions. The idea is that if a partial conversion occurs, then the converting the characters written by this function would leave the state in initial conditions, rather than partial conversion state. It does this by calling codecvt\+::do\+\_\+unshift().

For example, if 4 external characters always converted to 1 internal character, and input to \doxylink{_classes_2lib_2raylib_2src_2external_2glad_8h_a83ad0ee7f1e06b59c90271716e689080}{in()} had 6 external characters with state saved, this function would write two characters to the output and set the state to initialized conditions.

The source and destination character sets are determined by the facet\textquotesingle{}s locale, internal and external types.

The result returned is a member of codecvt\+\_\+base\+::result. If the state could be reset and data written, returns codecvt\+\_\+base\+::ok. If no conversion is necessary, returns codecvt\+\_\+base\+::noconv. If the output has insufficient space, returns codecvt\+\_\+base\+::partial. Otherwise the reset failed and codecvt\+\_\+base\+::error is returned.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+state} & Persistent conversion state data. \\
\hline
{\em \+\_\+\+\_\+to} & Start of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+end} & End of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+next} & Returns start of unused output area. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
codecvt\+\_\+base\+::result.
\end{DoxyReturn}
Convert from external to internal character set.

Converts input string of extern\+\_\+type to output string of intern\+\_\+type. This is analogous to mbsrtowcs. It does this by calling codecvt\+::do\+\_\+in.

The source and destination character sets are determined by the facet\textquotesingle{}s locale, internal and external types.

The characters in \mbox{[}from,from\+\_\+end) are converted and written to \mbox{[}to,to\+\_\+end). from\+\_\+next and to\+\_\+next are set to point to the character following the last successfully converted character, respectively. If the result needed no conversion, from\+\_\+next and to\+\_\+next are not affected.

The {\itshape state} argument should be initialized if the input is at the beginning and carried from a previous call if continuing conversion. There are no guarantees about how {\itshape state} is used.

The result returned is a member of codecvt\+\_\+base\+::result. If all the input is converted, returns codecvt\+\_\+base\+::ok. If no conversion is necessary, returns codecvt\+\_\+base\+::noconv. If the input ends early or there is insufficient space in the output, returns codecvt\+\_\+base\+::partial. Otherwise the conversion failed and codecvt\+\_\+base\+::error is returned.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+state} & Persistent conversion state data. \\
\hline
{\em \+\_\+\+\_\+from} & Start of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+end} & End of input. \\
\hline
{\em \+\_\+\+\_\+from\+\_\+next} & Returns start of unconverted data. \\
\hline
{\em \+\_\+\+\_\+to} & Start of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+end} & End of output buffer. \\
\hline
{\em \+\_\+\+\_\+to\+\_\+next} & Returns start of unused output area. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
codecvt\+\_\+base\+::result.
\end{DoxyReturn}
Convert from internal to external character set.

Converts input string of intern\+\_\+type to output string of extern\+\_\+type. This function is a hook for derived classes to change the value returned. \begin{DoxySeeAlso}{See also}
out for more information.
\end{DoxySeeAlso}
Primary class template codecvt.

NB\+: Generic, mostly useless implementation.

class codecvt$<$char, char, mbstate\+\_\+t$>$ specialization.

class codecvt\+\_\+byname \mbox{[}22.\+2.\+1.\+6\mbox{]}.

Container class for localization functionality.

The locale class is first a class wrapper for C library locales. It is also an extensible container for user-\/defined localization. A locale is a collection of facets that implement various localization features such as money, time, and number printing.

Constructing C++ locales does not change the C library locale.

This library supports efficient construction and copying of locales through a reference counting implementation of the locale class.

Definition of locale\+::category.

Category values.

The standard category values are none, ctype, numeric, collate, time, monetary, and messages. They form a bitmask that supports union and intersection. The category all is the union of these values.

NB\+: Order must match \+\_\+\+S\+\_\+facet\+\_\+categories definition in locale.\+cc

Common base for ctype facet

This template class provides implementations of the public functions that forward to the protected virtual functions.

This template also provides abstract stubs for the protected virtual functions.

Typedef for the template parameter

Test char\+\_\+type classification.

This function finds a mask M for {\itshape \+\_\+\+\_\+c} and compares it to mask {\itshape \+\_\+\+\_\+m}. It does so by returning the value of ctype$<$char\+\_\+type$>$\+::do\+\_\+is().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to compare the mask of. \\
\hline
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
(M \& \+\_\+\+\_\+m) != 0.
\end{DoxyReturn}
Return a mask array.

This function finds the mask for each char\+\_\+type in the range \mbox{[}lo,hi) and successively writes it to vec. vec must have as many elements as the char array. It does so by returning the value of ctype$<$char\+\_\+type$>$\+::do\+\_\+is().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+vec} & Pointer to an array of mask storage. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type matching a mask

This function searches for and returns the first char\+\_\+type c in \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,c)} is true. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+scan\+\_\+is().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to matching char\+\_\+type if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type not matching a mask

This function searches for and returns the first char\+\_\+type c in \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,c)} is false. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+scan\+\_\+not().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to non-\/matching char if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to uppercase.

This function converts the argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+toupper().


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This function converts each char\+\_\+type in the range \mbox{[}lo,hi) to uppercase if possible. Other elements remain untouched. It does so by returning ctype$<$char\+\_\+type$>$\+:: do\+\_\+toupper(lo, hi).


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This function converts the argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+tolower(c).


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to lowercase if possible. Other elements remain untouched. It does so by returning ctype$<$char\+\_\+type$>$\+:: do\+\_\+tolower(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi).


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char to char\+\_\+type

This function converts the char argument to char\+\_\+type using the simplest reasonable transformation. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+widen(c).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char\+\_\+type.
\end{DoxyReturn}
Widen array to char\+\_\+type

This function converts each char in the input to char\+\_\+type using the simplest reasonable transformation. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+widen(c).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char\+\_\+type to char

This function converts the char\+\_\+type to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+narrow(\+\_\+\+\_\+c).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char.
\end{DoxyReturn}
Narrow array to char array

This function converts each char\+\_\+type in the input to char using the simplest reasonable transformation and writes the results to the destination array. For any char\+\_\+type in the input that cannot be converted, {\itshape dfault} is used instead. It does so by returning ctype$<$char\+\_\+type$>$\+::do\+\_\+narrow(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi, \+\_\+\+\_\+dfault, \+\_\+\+\_\+to).

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Test char\+\_\+type classification.

This function finds a mask M for {\itshape c} and compares it to mask {\itshape m}.

do\+\_\+is() is a hook for a derived facet to change the behavior of classifying. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to find the mask of. \\
\hline
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
(M \& \+\_\+\+\_\+m) != 0.
\end{DoxyReturn}
Return a mask array.

This function finds the mask for each char\+\_\+type in the range \mbox{[}lo,hi) and successively writes it to vec. vec must have as many elements as the input.

do\+\_\+is() is a hook for a derived facet to change the behavior of classifying. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+vec} & Pointer to an array of mask storage. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type matching mask

This function searches for and returns the first char\+\_\+type c in \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(\+\_\+\+\_\+m,c)} is true.

do\+\_\+scan\+\_\+is() is a hook for a derived facet to change the behavior of match searching. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a matching char\+\_\+type if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char\+\_\+type not matching mask

This function searches for and returns a pointer to the first char\+\_\+type c of \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,c)} is false.

do\+\_\+scan\+\_\+is() is a hook for a derived facet to change the behavior of match searching. do\+\_\+is() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a non-\/matching char\+\_\+type if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to uppercase.

This virtual function converts the char\+\_\+type argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This virtual function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to uppercase if possible. Other elements remain untouched.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This virtual function converts the argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char\+\_\+type if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This virtual function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to lowercase if possible. Other elements remain untouched.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char

This virtual function converts the char to char\+\_\+type using the simplest reasonable transformation.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char\+\_\+type
\end{DoxyReturn}
Widen char array

This function converts each char in the input to char\+\_\+type using the simplest reasonable transformation.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char\+\_\+type to char

This virtual function converts the argument to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char\+\_\+type to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char.
\end{DoxyReturn}
Narrow char\+\_\+type array to char

This virtual function converts each char\+\_\+type in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to char using the simplest reasonable transformation and writes the results to the destination array. For any element in the input that cannot be converted, {\itshape \+\_\+\+\_\+dfault} is used instead.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Primary class template ctype facet.

This template class defines classification and conversion functions for character sets. It wraps cctype functionality. Ctype gets used by streams for many I/O operations.

This template provides the protected virtual functions the developer will have to replace in a derived class or specialization to make a working facet. The public functions that access them are defined in \+\_\+\+\_\+ctype\+\_\+abstract\+\_\+base, to allow for implementation flexibility. See ctype$<$wchar\+\_\+t$>$ for an example. The functions are documented in \+\_\+\+\_\+ctype\+\_\+abstract\+\_\+base.

Note\+: implementations are provided for all the protected virtual functions, but will likely not be useful.

The facet id for ctype$<$char\+\_\+type$>$

The ctype$<$char$>$ specialization.

This class defines classification and conversion functions for the char type. It gets used by char streams for many I/O operations. The char specialization provides a number of optimizations as well.

Typedef for the template parameter char.

The facet id for ctype$<$char$>$

The size of the mask table. It is SCHAR\+\_\+\+MAX + 1.

Constructor performs initialization.

This is the constructor provided by the standard.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+table} & If non-\/zero, table is used as the per-\/char mask. Else classic\+\_\+table() is used. \\
\hline
{\em \+\_\+\+\_\+del} & If true, passes ownership of table to this facet. \\
\hline
{\em \+\_\+\+\_\+refs} & Passed to the base facet class.\\
\hline
\end{DoxyParams}
Constructor performs static initialization.

This constructor is used to construct the initial C locale facet.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+cloc} & Handle to C locale data. \\
\hline
{\em \+\_\+\+\_\+table} & If non-\/zero, table is used as the per-\/char mask. \\
\hline
{\em \+\_\+\+\_\+del} & If true, passes ownership of table to this facet. \\
\hline
{\em \+\_\+\+\_\+refs} & Passed to the base facet class.\\
\hline
\end{DoxyParams}
Test char classification.

This function compares the mask table\mbox{[}c\mbox{]} to {\itshape \+\_\+\+\_\+m}.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to compare the mask of. \\
\hline
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
True if \+\_\+\+\_\+m \& table\mbox{[}\+\_\+\+\_\+c\mbox{]} is true, false otherwise.
\end{DoxyReturn}
Return a mask array.

This function finds the mask for each char in the range \mbox{[}lo, hi) and successively writes it to vec. vec must have as many elements as the char array.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+vec} & Pointer to an array of mask storage. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char matching a mask

This function searches for and returns the first char in \mbox{[}lo,hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,char)} is true.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a matching char if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Find char not matching a mask

This function searches for and returns a pointer to the first char in \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) for which \doxylink{_monobehavior_8h_a9870e0caa2f8e3e5700c4cbb21d73c07}{is(m,char)} is false.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+m} & The mask to compare against. \\
\hline
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
Pointer to a non-\/matching char if found, else {\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to uppercase.

This function converts the char argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

toupper() acts as if it returns ctype$<$char$>$\+::do\+\_\+toupper(c). do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This function converts each char in the range \mbox{[}\+\_\+\+\_\+lo,\+\_\+\+\_\+hi) to uppercase if possible. Other chars remain untouched.

toupper() acts as if it returns ctype$<$char$>$\+:: do\+\_\+toupper(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi). do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This function converts the char argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

tolower() acts as if it returns ctype$<$char$>$\+::do\+\_\+tolower(\+\_\+\+\_\+c). do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This function converts each char in the range \mbox{[}lo,hi) to lowercase if possible. Other chars remain untouched.

tolower() acts as if it returns ctype$<$char$>$\+:: do\+\_\+tolower(\+\_\+\+\_\+lo, \+\_\+\+\_\+hi). do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char

This function converts the char to char\+\_\+type using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be returned unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+widen(c). do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted character.
\end{DoxyReturn}
Widen char array

This function converts each char in the input to char using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+widen(c). do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char

This function converts the char to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead. For an underived ctype$<$char$>$ facet, {\itshape c} will be returned unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+narrow(c). do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted character.
\end{DoxyReturn}
Narrow char array

This function converts each char in the input to char using the simplest reasonable transformation and writes the results to the destination array. For any char in the input that cannot be converted, {\itshape dfault} is used instead. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

This function works as if it returns ctype$<$char$>$\+::do\+\_\+narrow(lo, hi, dfault, to). do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Returns a pointer to the mask table provided to the constructor, or the default from classic\+\_\+table() if none was provided.

Returns a pointer to the C locale mask table.

Destructor.

This function deletes \doxylink{classtable}{table()} if {\itshape del} was true in the constructor.

Convert to uppercase.

This virtual function converts the char argument to uppercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The uppercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to uppercase.

This virtual function converts each char in the range \mbox{[}lo,hi) to uppercase if possible. Other chars remain untouched.

do\+\_\+toupper() is a hook for a derived facet to change the behavior of uppercasing. do\+\_\+toupper() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Convert to lowercase.

This virtual function converts the char argument to lowercase if possible. If not possible (for example, \textquotesingle{}2\textquotesingle{}), returns the argument.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The lowercase char if convertible, else {\itshape \+\_\+\+\_\+c}.
\end{DoxyReturn}
Convert array to lowercase.

This virtual function converts each char in the range \mbox{[}lo,hi) to lowercase if possible. Other chars remain untouched.

do\+\_\+tolower() is a hook for a derived facet to change the behavior of lowercasing. do\+\_\+tolower() must always return the same result for the same input.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to first char in range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Widen char

This virtual function converts the char to char using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be returned unchanged.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted character.
\end{DoxyReturn}
Widen char array

This function converts each char in the range \mbox{[}lo,hi) to char using the simplest reasonable transformation. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

do\+\_\+widen() is a hook for a derived facet to change the behavior of widening. do\+\_\+widen() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
Narrow char

This virtual function converts the char to char using the simplest reasonable transformation. If the conversion fails, dfault is returned instead. For an underived ctype$<$char$>$ facet, {\itshape c} will be returned unchanged.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+c} & The char to convert. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to return if conversion fails. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
The converted char.
\end{DoxyReturn}
Narrow char array to char array

This virtual function converts each char in the range \mbox{[}lo,hi) to char using the simplest reasonable transformation and writes the results to the destination array. For any char in the input that cannot be converted, {\itshape dfault} is used instead. For an underived ctype$<$char$>$ facet, the argument will be copied unchanged.

do\+\_\+narrow() is a hook for a derived facet to change the behavior of narrowing. do\+\_\+narrow() must always return the same result for the same input.

Note\+: this is not what you want for codepage conversions. See codecvt for that.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+lo} & Pointer to start of range. \\
\hline
{\em \+\_\+\+\_\+hi} & Pointer to end of range. \\
\hline
{\em \+\_\+\+\_\+dfault} & Char to use if conversion fails. \\
\hline
{\em \+\_\+\+\_\+to} & Pointer to the destination array. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
{\itshape \+\_\+\+\_\+hi}.
\end{DoxyReturn}
class ctype\+\_\+byname \mbox{[}22.\+2.\+1.\+2\mbox{]}.

22.\+2.\+1.\+4 Class ctype\+\_\+byname specializations.

Primary class template numpunct.

This facet stores several pieces of information related to printing and scanning numbers, such as the decimal point character. It takes a template parameter specifying the char type. The numpunct facet is used by streams for many I/O operations involving numbers.

The numpunct template uses protected virtual functions to provide the actual results. The public accessors forward the call to the virtual functions. These virtual functions are hooks for developers to implement the behavior they require from a numpunct facet.

Public typedefs

Time format ordering data.

This class provides an enum representing different orderings of time\+: day, month, and year.

Numpunct facet id.

Internal constructor. Not for general use.

This is a constructor for use by the library itself to set up new locales.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+cloc} & The C locale. \\
\hline
{\em \+\_\+\+\_\+s} & The name of a locale. \\
\hline
{\em refs} & Passed to the base facet class.\\
\hline
\end{DoxyParams}
Primary class template time\+\_\+get.

This facet encapsulates the code to parse and return a date or time from a string. It is used by the istream numeric extraction operators.

The time\+\_\+get template uses protected virtual functions to provide the actual results. The public accessors forward the call to the virtual functions. These virtual functions are hooks for developers to implement the behavior they require from the time\+\_\+get facet.

Public typedefsHere is the call graph for this function\+:
% FIG 0
