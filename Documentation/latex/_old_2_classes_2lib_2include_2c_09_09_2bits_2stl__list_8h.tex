\doxysection{C\+:/\+Users/\+HP/\+Documents/\+Git\+Hub/\+Minero-\/\+Game/\+Old/\+Classes/lib/include/c++/bits/stl\+\_\+list.h File Reference}
\hypertarget{_old_2_classes_2lib_2include_2c_09_09_2bits_2stl__list_8h}{}\label{_old_2_classes_2lib_2include_2c_09_09_2bits_2stl__list_8h}\index{C:/Users/HP/Documents/GitHub/Minero-\/Game/Old/Classes/lib/include/c++/bits/stl\_list.h@{C:/Users/HP/Documents/GitHub/Minero-\/Game/Old/Classes/lib/include/c++/bits/stl\_list.h}}
{\ttfamily \#include $<$bits/concept\+\_\+check.\+h$>$}\newline
{\ttfamily \#include $<$ext/alloc\+\_\+traits.\+h$>$}\newline
Include dependency graph for stl\+\_\+list.\+h\+:
% FIG 0
\doxysubsubsection*{Macros}
\begin{DoxyCompactItemize}
\item 
\#\mbox{\hyperlink{_classes_2lib_2include_2avx512fintrin_8h_a1b7950c88593de42a92cc8558f5488f6}{define}} \mbox{\hyperlink{_old_2_classes_2lib_2include_2c_09_09_2bits_2stl__list_8h_a0a6e3cfff4c3dc6aeaeaf4edd2e19b69}{\+\_\+\+GLIBCXX\+\_\+\+LIST\+\_\+\+REMOVE\+\_\+\+RETURN\+\_\+\+TYPE\+\_\+\+TAG}}
\end{DoxyCompactItemize}
\doxysubsubsection*{Functions}
\begin{DoxyCompactItemize}
\item 
namespace std \mbox{\hyperlink{_old_2_classes_2lib_2include_2c_09_09_2bits_2stl__list_8h_a544d74b2d5d0dd2197f5fd6f02691de0}{\+\_\+\+GLIBCXX\+\_\+\+VISIBILITY}} (default)
\end{DoxyCompactItemize}


\doxysubsection{Macro Definition Documentation}
\Hypertarget{_old_2_classes_2lib_2include_2c_09_09_2bits_2stl__list_8h_a0a6e3cfff4c3dc6aeaeaf4edd2e19b69}\label{_old_2_classes_2lib_2include_2c_09_09_2bits_2stl__list_8h_a0a6e3cfff4c3dc6aeaeaf4edd2e19b69} 
\index{stl\_list.h@{stl\_list.h}!\_GLIBCXX\_LIST\_REMOVE\_RETURN\_TYPE\_TAG@{\_GLIBCXX\_LIST\_REMOVE\_RETURN\_TYPE\_TAG}}
\index{\_GLIBCXX\_LIST\_REMOVE\_RETURN\_TYPE\_TAG@{\_GLIBCXX\_LIST\_REMOVE\_RETURN\_TYPE\_TAG}!stl\_list.h@{stl\_list.h}}
\doxysubsubsection{\texorpdfstring{\_GLIBCXX\_LIST\_REMOVE\_RETURN\_TYPE\_TAG}{\_GLIBCXX\_LIST\_REMOVE\_RETURN\_TYPE\_TAG}}
{\footnotesize\ttfamily \#\mbox{\hyperlink{_classes_2lib_2include_2avx512fintrin_8h_a1b7950c88593de42a92cc8558f5488f6}{define}} \+\_\+\+GLIBCXX\+\_\+\+LIST\+\_\+\+REMOVE\+\_\+\+RETURN\+\_\+\+TYPE\+\_\+\+TAG}



\doxysubsection{Function Documentation}
\Hypertarget{_old_2_classes_2lib_2include_2c_09_09_2bits_2stl__list_8h_a544d74b2d5d0dd2197f5fd6f02691de0}\label{_old_2_classes_2lib_2include_2c_09_09_2bits_2stl__list_8h_a544d74b2d5d0dd2197f5fd6f02691de0} 
\index{stl\_list.h@{stl\_list.h}!\_GLIBCXX\_VISIBILITY@{\_GLIBCXX\_VISIBILITY}}
\index{\_GLIBCXX\_VISIBILITY@{\_GLIBCXX\_VISIBILITY}!stl\_list.h@{stl\_list.h}}
\doxysubsubsection{\texorpdfstring{\_GLIBCXX\_VISIBILITY()}{\_GLIBCXX\_VISIBILITY()}}
{\footnotesize\ttfamily namespace std \+\_\+\+GLIBCXX\+\_\+\+VISIBILITY (\begin{DoxyParamCaption}\item[{default}]{ }\end{DoxyParamCaption})}

Common part of a node in the list.

The list node header.

An actual node in the list.

A list\+::iterator.

All the functions are op overloads.

A list\+::const\+\_\+iterator.

All the functions are op overloads.

See bits/stl\+\_\+deque.\+h\textquotesingle{}s \+\_\+\+Deque\+\_\+base for an explanation.

A standard container with linear time access to elements, and fixed time insertion/deletion at any point in the sequence.


\begin{DoxyTemplParams}{Template Parameters}
{\em \+\_\+\+Tp} & Type of element. \\
\hline
{\em \+\_\+\+Alloc} & Allocator type, defaults to allocator$<$\+\_\+\+Tp$>$.\\
\hline
\end{DoxyTemplParams}
Meets the requirements of a \href{tables.html\#65}{\texttt{ container}}, a \href{tables.html\#66}{\texttt{ reversible container}}, and a \href{tables.html\#67}{\texttt{ sequence}}, including the \href{tables.html\#68}{\texttt{ optional sequence requirements}} with the exception of {\ttfamily at} and {\ttfamily operator}\mbox{[}\mbox{]}.

This is a {\itshape doubly} {\itshape linked} list. Traversal up and down the list requires linear time, but adding and removing elements (or {\itshape nodes}) is done in constant time, regardless of where the change takes place. Unlike std\+::vector and std\+::deque, random-\/access iterators are not provided, so subscripting ( {\ttfamily }\mbox{[}\mbox{]} ) access is not allowed. For algorithms which only need sequential access, this lack makes no difference.

Also unlike the other standard containers, std\+::list provides specialized algorithms unique to linked lists, such as splicing, sorting, and in-\/place reversal.

A couple points on memory allocation for list$<$\+Tp$>$\+:

First, we never actually allocate a Tp, we allocate List\+\_\+node$<$\+Tp$>$\textquotesingle{}s and trust \mbox{[}20.\+1.\+5\mbox{]}/4 to DTRT. This is to ensure that after elements from list\texorpdfstring{$<$}{<}X,Alloc1\texorpdfstring{$>$}{>} are spliced into list\texorpdfstring{$<$}{<}X,Alloc2\texorpdfstring{$>$}{>}, destroying the memory of the second list is a valid operation, i.\+e., Alloc1 giveth and Alloc2 taketh away.

Second, a list conceptually represented as 
\begin{DoxyCode}{0}
\DoxyCodeLine{A\ <-\/-\/-\/>\ B\ <-\/-\/-\/>\ \mbox{\hyperlink{_classes_2lib_2external_2stb__vorbis_8c_ac4cf4b2ab929bd23951a8676eeac086b}{C}}\ <-\/-\/-\/>\ D}

\end{DoxyCode}
 is actually circular; a link exists between A and D. The list class holds (as its only data member) a private list\+::iterator pointing to {\itshape D}, not to {\itshape A!} To get to the head of the list, we start at the tail and move forward by one. When this member iterator\textquotesingle{}s next/previous pointers refer to itself, the list is empty.


\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+args} & An instance of user data.\\
\hline
\end{DoxyParams}
Allocates space for a new node and constructs a copy of {\itshape \+\_\+\+\_\+args} in it.

Creates a list with no elements.

Creates a list with no elements. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+a} & An allocator object.\\
\hline
\end{DoxyParams}
Creates a list with copies of an exemplar element. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+n} & The number of elements to initially create. \\
\hline
{\em \+\_\+\+\_\+value} & An element to copy. \\
\hline
{\em \+\_\+\+\_\+a} & An allocator object.\\
\hline
\end{DoxyParams}
This constructor fills the list with {\itshape \+\_\+\+\_\+n} copies of {\itshape \+\_\+\+\_\+value}.

List copy constructor. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+x} & A list of identical element and allocator types.\\
\hline
\end{DoxyParams}
The newly-\/created list uses a copy of the allocation object used by {\itshape \+\_\+\+\_\+x} (unless the allocator traits dictate a different object).

Builds a list from a range. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+first} & An input iterator. \\
\hline
{\em \+\_\+\+\_\+last} & An input iterator. \\
\hline
{\em \+\_\+\+\_\+a} & An allocator object.\\
\hline
\end{DoxyParams}
Create a list consisting of copies of the elements from \mbox{[}{\itshape \+\_\+\+\_\+first},{\itshape \+\_\+\+\_\+last}). This is linear in N (where N is distance({\itshape \+\_\+\+\_\+first},{\itshape \+\_\+\+\_\+last})).

List assignment operator. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+x} & A list of identical element and allocator types.\\
\hline
\end{DoxyParams}
All the elements of {\itshape \+\_\+\+\_\+x} are copied.

Whether the allocator is copied depends on the allocator traits.

Assigns a given value to a list. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+n} & Number of elements to be assigned. \\
\hline
{\em \+\_\+\+\_\+val} & Value to be assigned.\\
\hline
\end{DoxyParams}
This function fills a list with {\itshape \+\_\+\+\_\+n} copies of the given value. Note that the assignment completely changes the list and that the resulting list\textquotesingle{}s size is the same as the number of elements assigned.

Assigns a range to a list. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+first} & An input iterator. \\
\hline
{\em \+\_\+\+\_\+last} & An input iterator.\\
\hline
\end{DoxyParams}
This function fills a list with copies of the elements in the range \mbox{[}{\itshape \+\_\+\+\_\+first},{\itshape \+\_\+\+\_\+last}).

Note that the assignment completely changes the list and that the resulting list\textquotesingle{}s size is the same as the number of elements assigned.

Get a copy of the memory allocation object.

Returns a read/write iterator that points to the first element in the list. Iteration is done in ordinary element order.

Returns a read-\/only (constant) iterator that points to the first element in the list. Iteration is done in ordinary element order.

Returns a read/write iterator that points one past the last element in the list. Iteration is done in ordinary element order.

Returns a read-\/only (constant) iterator that points one past the last element in the list. Iteration is done in ordinary element order.

Returns a read/write reverse iterator that points to the last element in the list. Iteration is done in reverse element order.

Returns a read-\/only (constant) reverse iterator that points to the last element in the list. Iteration is done in reverse element order.

Returns a read/write reverse iterator that points to one before the first element in the list. Iteration is done in reverse element order.

Returns a read-\/only (constant) reverse iterator that points to one before the first element in the list. Iteration is done in reverse element order.

Returns true if the list is empty. (Thus begin() would equal \doxylink{_classes_2lib_2raylib_2src_2external_2glad_8h_a432111147038972f06e049e18a837002}{end()}.)

Returns the number of elements in the list. ~\newline


Returns the \doxylink{_classes_2lib_2raylib_2src_2external_2_a_n_g_l_e_2_g_l_e_s2_2gl2ext_8h_a3d1e3edfcf61ca2d831883e1afbad89e}{size()} of the largest possible list. ~\newline


Resizes the list to the specified number of elements. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+new\+\_\+size} & Number of elements the list should contain. \\
\hline
{\em \+\_\+\+\_\+x} & Data with which new elements should be populated.\\
\hline
\end{DoxyParams}
This function will resize the list to the specified number of elements. If the number is smaller than the list\textquotesingle{}s current size the list is truncated, otherwise the list is extended and new elements are populated with given data.

Returns a read/write reference to the data at the first element of the list.

Returns a read-\/only (constant) reference to the data at the first element of the list.

Returns a read/write reference to the data at the last element of the list.

Returns a read-\/only (constant) reference to the data at the last element of the list.

Add data to the front of the list. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+x} & Data to be added.\\
\hline
\end{DoxyParams}
This is a typical stack operation. The function creates an element at the front of the list and assigns the given data to it. Due to the nature of a list this operation can be done in constant time, and does not invalidate iterators and references.

Removes first element.

This is a typical stack operation. It shrinks the list by one. Due to the nature of a list this operation can be done in constant time, and only invalidates iterators/references to the element being removed.

Note that no data is returned, and if the first element\textquotesingle{}s data is needed, it should be retrieved before pop\+\_\+front() is called.

Add data to the end of the list. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+x} & Data to be added.\\
\hline
\end{DoxyParams}
This is a typical stack operation. The function creates an element at the end of the list and assigns the given data to it. Due to the nature of a list this operation can be done in constant time, and does not invalidate iterators and references.

Removes last element.

This is a typical stack operation. It shrinks the list by one. Due to the nature of a list this operation can be done in constant time, and only invalidates iterators/references to the element being removed.

Note that no data is returned, and if the last element\textquotesingle{}s data is needed, it should be retrieved before pop\+\_\+back() is called.

Inserts given value into list before specified iterator. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+position} & An iterator into the list. \\
\hline
{\em \+\_\+\+\_\+x} & Data to be inserted. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
An iterator that points to the inserted data.
\end{DoxyReturn}
This function will insert a copy of the given value before the specified location. Due to the nature of a list this operation can be done in constant time, and does not invalidate iterators and references.

Inserts a number of copies of given data into the list. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+position} & An iterator into the list. \\
\hline
{\em \+\_\+\+\_\+n} & Number of elements to be inserted. \\
\hline
{\em \+\_\+\+\_\+x} & Data to be inserted.\\
\hline
\end{DoxyParams}
This function will insert a specified number of copies of the given data before the location specified by {\itshape position}.

This operation is linear in the number of elements inserted and does not invalidate iterators and references.

Inserts a range into the list. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+position} & An iterator into the list. \\
\hline
{\em \+\_\+\+\_\+first} & An input iterator. \\
\hline
{\em \+\_\+\+\_\+last} & An input iterator.\\
\hline
\end{DoxyParams}
This function will insert copies of the data in the range \mbox{[}{\itshape first},{\itshape last}) into the list before the location specified by {\itshape position}.

This operation is linear in the number of elements inserted and does not invalidate iterators and references.

Remove element at given position. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+position} & Iterator pointing to element to be erased. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
An iterator pointing to the next element (or \doxylink{_classes_2lib_2raylib_2src_2external_2glad_8h_a432111147038972f06e049e18a837002}{end()}).
\end{DoxyReturn}
This function will erase the element at the given position and thus shorten the list by one.

Due to the nature of a list this operation can be done in constant time, and only invalidates iterators/references to the element being removed. The user is also cautioned that this function only erases the element, and that if the element is itself a pointer, the pointed-\/to memory is not touched in any way. Managing the pointer is the user\textquotesingle{}s responsibility.

Remove a range of elements. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+first} & Iterator pointing to the first element to be erased. \\
\hline
{\em \+\_\+\+\_\+last} & Iterator pointing to one past the last element to be erased. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
An iterator pointing to the element pointed to by {\itshape last} prior to erasing (or \doxylink{_classes_2lib_2raylib_2src_2external_2glad_8h_a432111147038972f06e049e18a837002}{end()}).
\end{DoxyReturn}
This function will erase the elements in the range {\itshape }\mbox{[}first,last) and shorten the list accordingly.

This operation is linear time in the size of the range and only invalidates iterators/references to the element being removed. The user is also cautioned that this function only erases the elements, and that if the elements themselves are pointers, the pointed-\/to memory is not touched in any way. Managing the pointer is the user\textquotesingle{}s responsibility.

Swaps data with another list. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+x} & A list of the same element and allocator types.\\
\hline
\end{DoxyParams}
This exchanges the elements between two lists in constant time. Note that the global \doxylink{namespacestd_a3ece115a1c8ed9020a20f1d0b70efda8}{std\+::swap()} function is specialized such that std\+::swap(l1,l2) will feed to this function.

Whether the allocators are swapped depends on the allocator traits.

Erases all the elements. Note that this function only erases the elements, and that if the elements themselves are pointers, the pointed-\/to memory is not touched in any way. Managing the pointer is the user\textquotesingle{}s responsibility.

Insert contents of another list. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+position} & Iterator referencing the element to insert before. \\
\hline
{\em \+\_\+\+\_\+x} & Source list.\\
\hline
\end{DoxyParams}
The elements of {\itshape \+\_\+\+\_\+x} are inserted in constant time in front of the element referenced by {\itshape \+\_\+\+\_\+position}. {\itshape \+\_\+\+\_\+x} becomes an empty list.

Requires this != {\itshape \+\_\+\+\_\+x}.

Insert element from another list. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+position} & Iterator referencing the element to insert before. \\
\hline
{\em \+\_\+\+\_\+x} & Source list. \\
\hline
{\em \+\_\+\+\_\+i} & Iterator referencing the element to move.\\
\hline
\end{DoxyParams}
Removes the element in list {\itshape \+\_\+\+\_\+x} referenced by {\itshape \+\_\+\+\_\+i} and inserts it into the current list before {\itshape \+\_\+\+\_\+position}.

Insert range from another list. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+position} & Iterator referencing the element to insert before. \\
\hline
{\em \+\_\+\+\_\+x} & Source list. \\
\hline
{\em \+\_\+\+\_\+first} & Iterator referencing the start of range in x. \\
\hline
{\em \+\_\+\+\_\+last} & Iterator referencing the end of range in x.\\
\hline
\end{DoxyParams}
Removes elements in the range \mbox{[}\+\_\+\+\_\+first,\+\_\+\+\_\+last) and inserts them before {\itshape \+\_\+\+\_\+position} in constant time.

Undefined if {\itshape \+\_\+\+\_\+position} is in \mbox{[}\+\_\+\+\_\+first,\+\_\+\+\_\+last).

Remove all elements equal to value. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+value} & The value to remove.\\
\hline
\end{DoxyParams}
Removes every element in the list equal to {\itshape value}. Remaining elements stay in list order. Note that this function only erases the elements, and that if the elements themselves are pointers, the pointed-\/to memory is not touched in any way. Managing the pointer is the user\textquotesingle{}s responsibility.

Remove all elements satisfying a predicate. 
\begin{DoxyTemplParams}{Template Parameters}
{\em \+\_\+\+Predicate} & Unary predicate function or object.\\
\hline
\end{DoxyTemplParams}
Removes every element in the list for which the predicate returns true. Remaining elements stay in list order. Note that this function only erases the elements, and that if the elements themselves are pointers, the pointed-\/to memory is not touched in any way. Managing the pointer is the user\textquotesingle{}s responsibility.

Remove consecutive duplicate elements.

For each consecutive set of elements with the same value, remove all but the first one. Remaining elements stay in list order. Note that this function only erases the elements, and that if the elements themselves are pointers, the pointed-\/to memory is not touched in any way. Managing the pointer is the user\textquotesingle{}s responsibility.

Remove consecutive elements satisfying a predicate. 
\begin{DoxyTemplParams}{Template Parameters}
{\em \+\_\+\+Binary\+Predicate} & Binary predicate function or object.\\
\hline
\end{DoxyTemplParams}
For each consecutive set of elements \mbox{[}first,last) that satisfy predicate(first,i) where i is an iterator in \mbox{[}first,last), remove all but the first one. Remaining elements stay in list order. Note that this function only erases the elements, and that if the elements themselves are pointers, the pointed-\/to memory is not touched in any way. Managing the pointer is the user\textquotesingle{}s responsibility.

Merge sorted lists. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+x} & Sorted list to merge.\\
\hline
\end{DoxyParams}
Assumes that both {\itshape \+\_\+\+\_\+x} and this list are sorted according to operator$<$(). Merges elements of {\itshape \+\_\+\+\_\+x} into this list in sorted order, leaving {\itshape \+\_\+\+\_\+x} empty when complete. Elements in this list precede elements in {\itshape \+\_\+\+\_\+x} that are equal.

Merge sorted lists according to comparison function. 
\begin{DoxyTemplParams}{Template Parameters}
{\em \+\_\+\+Strict\+Weak\+Ordering} & Comparison function defining sort order. \\
\hline
\end{DoxyTemplParams}

\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+x} & Sorted list to merge. \\
\hline
{\em \+\_\+\+\_\+comp} & Comparison functor.\\
\hline
\end{DoxyParams}
Assumes that both {\itshape \+\_\+\+\_\+x} and this list are sorted according to Strict\+Weak\+Ordering. Merges elements of {\itshape \+\_\+\+\_\+x} into this list in sorted order, leaving {\itshape \+\_\+\+\_\+x} empty when complete. Elements in this list precede elements in {\itshape \+\_\+\+\_\+x} that are equivalent according to Strict\+Weak\+Ordering().

Reverse the elements in list.

Reverse the order of elements in the list in linear time.

Sort the elements.

Sorts the elements of this list in NlogN time. Equivalent elements remain in list order.

Sort the elements according to comparison function.

Sorts the elements of this list in NlogN time. Equivalent elements remain in list order.

List equality comparison. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+x} & A list. \\
\hline
{\em \+\_\+\+\_\+y} & A list of the same type as {\itshape \+\_\+\+\_\+x}. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
True iff the size and elements of the lists are equal.
\end{DoxyReturn}
This is an equivalence relation. It is linear in the size of the lists. Lists are considered equivalent if their sizes are equal, and if corresponding elements compare equal.

List ordering relation. 
\begin{DoxyParams}{Parameters}
{\em \+\_\+\+\_\+x} & A list. \\
\hline
{\em \+\_\+\+\_\+y} & A list of the same type as {\itshape \+\_\+\+\_\+x}. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
True iff {\itshape \+\_\+\+\_\+x} is lexicographically less than {\itshape \+\_\+\+\_\+y}.
\end{DoxyReturn}
This is a total ordering relation. It is linear in the size of the lists. The elements must be comparable with {\ttfamily \texorpdfstring{$<$}{<}}.

See \doxylink{namespacestd_a076cfce541233dc2534e44d067d41ad4}{std\+::lexicographical\+\_\+compare()} for how the determination is made.

Based on operator==

Based on operator\texorpdfstring{$<$}{<}

Based on operator\texorpdfstring{$<$}{<}

Based on operator\texorpdfstring{$<$}{<}

See std\+::list\+::swap().Here is the call graph for this function\+:
% FIG 1
