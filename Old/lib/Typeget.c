#include "Typeget.h"
#include "stdio.h"
#include "stdlib.h"
#include "stdbool.h"


#ifdef TYPEGET_H

    bool IsofType(void *argument) {
       if (&argument != class) {
        malloc(sizeof(&argument));

        

        free(Type);
        free(argument);
       } else {
        
       }
        

    }
#else
    _THROW("TYPEGET_H not defined!");
#endif
